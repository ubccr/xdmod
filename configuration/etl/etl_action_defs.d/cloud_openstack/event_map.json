{
    "#": "This action loads data from a structured file and only needs the table definition",
    "table_definition": [
        {
            "$ref": "${table_definition_dir}/cloud_openstack/openstack_event_map.json#/table_definition"
        }
    ]
}
