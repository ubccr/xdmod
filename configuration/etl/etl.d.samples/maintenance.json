{
    "#": "Example of various maintenance tools including table management and custom SQL.",

    "defaults": {

        "#": "Options specific to the 'maintenance' pipeline",
        "maintenance": {
            "namespace": "ETL\\Maintenance",
            "options_class": "MaintenanceOptions"
        }
    },

    "maintenance": [{
        "#": "Manage one or more tables not managed by another action. With the exception of",
        "#": "the table_definition key, all other keys/directives in the table definition files",
        "#": "are ignored.",

        "name": "TableManagement",
        "class": "ManageTables",

        "#": "One or more table definition files are processed. Relative paths are prefixed by",
        "#": "the defined table_definition path.",

        "table_def_list": [
            "jobfactstatus.json"
        ],
        "enabled": true
    }, {
        "name": "CustomTabSetup",
        "description": "Perform setup for the Custom Queries tab",
        "class": "ExecuteSql",

        "#": "One or more custom SQL scripts are processed. If a delimiter is specified it",
        "#": "is used to break apart individual queries in the file that are then executed",
        "#": "independently. Macros defined by the ExecuteSql action may also be available.",
        "#": "This is also useful for post-processing data generated by an action.",

        "sql_file_list": [{
            "sql_file": "custom_tab_setup.sql",
            "delimiter": "//"
        }],
        "enabled": true
    }]
}
