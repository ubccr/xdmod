{
    "$schema": "http://json-schema.org/draft-07/schema",
    "title": "create_user_token_failure",
    "type": "object",
    "properties": {
        "success": {
            "enum": [false]
        },
        "message": {
           "enum": [
               "Expiration Interval not provided.",
               "Unable to create a new API token.",
               "Unable to create a new token at this time."
           ]
        }
    }
}
