{
    "$schema": "http://json-schema.org/draft-07/schema",
    "title": "revoke_user_token_failure",
    "type": "object",
    "properties": {
        "success": {
            "enum": [false]
        },
        "message": {
            "enum": [
                "Unable to revoke API Token.",
                "Unable to successfully revoke token.",
                "No token to revoke."

            ]
        }
    }
}
